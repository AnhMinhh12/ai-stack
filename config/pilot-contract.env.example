# Pilot external contracts. Copy into a protected environment; do not commit values.
# A value must be a secret-manager reference, never an inline credential.
SECRET_MANAGER_URI=
VLLM_API_KEY_REF=
QDRANT_API_KEY_REF=
REDIS_PASSWORD_REF=
WEBUI_SECRET_KEY_REF=
OIDC_ENABLED=false
OIDC_ISSUER_URL=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET_REF=
OIDC_REDIRECT_URL=
OIDC_GROUP_CLAIM=groups
OIDC_CA_BUNDLE_REF=
ALERT_WEBHOOK_REF=
ONCALL_OWNER=
TRACE_RETENTION_DAYS=
OFFHOST_BACKUP_URI=
